# entropy-table

Contract-first repository for a relational entropy atlas.

## Quickstart

```bash
python tools/validate.py
pytest
python tools/render.py
```

Rendered artifacts are written to `outputs/` and are intentionally not committed.
